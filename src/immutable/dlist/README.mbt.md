# Difference List

A tree-based linear data structure supporting efficient concatenation.
Note that the tree is not balanced, so a worst case would take time
proportional to the length of the list to retrieve the first or the last element.
