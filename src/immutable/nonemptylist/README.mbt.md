# Non-empty List

A non-empty linked list, which is essentially a head element plus a `@list.List`.
Functions `filter`, `remove`, `take` etc. may yield an empty list and are hence
not provided. If you need these operations, convert the non-empty list into
an ordinary list.
